<%= form_for(@coaching) do |f| %>
  <% if @coaching.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coaching.errors.count, "error") %> prohibited this coaching from being saved:</h2>

      <ul>
      <% @coaching.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.number_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :coach_id %><br>
    <%= f.number_field :coach_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
